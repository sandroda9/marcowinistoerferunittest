version: 2.1

jobs:
  build:
    working_directory: ~/project
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Print Working Directory
          command: pwd
      - run:
          name: List All Files in Project Directory
          command: ls -al /home/circleci/project
      - run:
          name: List All Files in Unittest Directory
          command: ls -al /home/circleci/project/unittest || echo "Directory unittest does not exist"
      - run:
          name: Build Project
          command: mvn -f /home/circleci/project/pom.xml install -DskipTests || mvn -f /home/circleci/project/unittest/pom.xml install -DskipTests

  unit_test:
    working_directory: ~/project/unittest
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Print Current Directory
          command: pwd
      - run:
          name: Run Unit Tests
          command: mvn -f ~/project/unittest/pom.xml test -Dtest=com.mayab.quality.unittest.service.UserServiceTest

  integration_test:
    working_directory: ~/project/unittest
    docker:
      - image: cimg/openjdk:11.0
      - image: circleci/mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: calidad2024
      MYSQL_USER: root
      MYSQL_PASSWORD: root
    steps:
      - checkout
      - run:
          name: Wait for MySQL to Start
          command: |
            for i in `seq 1 30`; do
              nc -z localhost 3306 && echo "MySQL is up!" && break
              echo "Waiting for MySQL..."
              sleep 1
            done
      - run:
          name: Run Integration Tests
          command: mvn -f ~/project/unittest/pom.xml failsafe:integration-test -Dit.test=com.mayab.quality.integration.UserServiceTest

  selenium_test:
    working_directory: ~/project/unittest
    docker:
      - image: cimg/openjdk:11.0
      - image: selenium/standalone-chrome:latest
    steps:
      - checkout
      - run:
          name: Run Selenium Functional Tests
          command: mvn -f ~/project/unittest/pom.xml verify -P selenium -Dtest=com.mayab.quality.functional.CRUDSeleniumTest

workflows:
  build_and_test:
    jobs:
      - build
      - unit_test:
          requires:
            - build
      - integration_test:
          requires:
            - unit_test
      - selenium_test:
          requires:
            - integration_test
